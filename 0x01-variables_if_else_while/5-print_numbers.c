#include <stdio.h>
/**
 * main - Function to print single digits of base 10
 *
 * Return: Always (0)
 */
int main(void)
{
	int a = 0;

	while ( a <= 9 )
	{
		printf("%d", a);
		a++;
	}
	printf("\n");
	return (0);
}
