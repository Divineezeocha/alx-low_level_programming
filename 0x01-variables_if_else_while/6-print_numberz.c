#include <stdio.h>
/**
 * main - Function to print single number of base 10
 *
 * Return: Always (0)
 */
int main(void)
{
	int  a = 0;

	while (a < 10)
	{
		putchar(a);
		a++;
	}
	putchar('\n');
	return (0);
}
